import { PositionType } from '@antv/l7-core';

/**
 * ZoomControl
 */
export interface IZoomControlOption {
  position: PositionType;
  zoomInText: string;
  zoomInTitle: string;
  zoomOutText: string;
  zoomOutTitle: string;
}
